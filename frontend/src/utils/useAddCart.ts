import { useDispatch } from 'react-redux';
import { addToCart } from '../services/cart/cartAction';

export const useAddCart = (cart: any[], productById: any) => {

  const dispatch = useDispatch();
  
}